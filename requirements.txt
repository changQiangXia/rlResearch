# RL_Evolution 项目依赖
# 安装命令: pip install -r requirements.txt

# 核心依赖
numpy>=1.20.0,<1.22.0          # 数值计算 (避免与 Python 3.7 冲突)
torch>=1.13.0,<2.0.0           # PyTorch 深度学习框架
gym==0.21.0                    # OpenAI Gym (强化学习环境)

# 可视化和日志
matplotlib>=3.3.0              # 绘图库
seaborn>=0.11.0                # 高级可视化
tensorboard>=2.8.0             # TensorBoard 日志

# 可选依赖 (用于特定环境)
# box2d-py>=2.3.8              # Box2D 物理引擎 (LunarLander, BipedalWalker)
# pygame>=2.1.0                # 游戏渲染 (部分环境需要)

# 开发工具 (可选)
# jupyter>=1.0.0               # Jupyter Notebook
# ipython>=7.0.0               # 增强型 Python 交互式解释器
